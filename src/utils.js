var rootUrl = 'https://skillsmgr.firebaseio.com/'; //Firebase connection

function capitalizeFirstLetter(string) {
    'use strict';
    return string.substr(0, 1).toUpperCase() + string.substr(1).toLowerCase();
}