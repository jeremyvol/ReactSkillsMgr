var React = require('react');

module.exports = React.createClass({
    render: function () {
        return <div className="">
            <h2 className="text-center">Associer une compétence à un consultant</h2>
        </div>
    }
});